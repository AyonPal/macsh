# Swift / SwiftPM
.build/
.swiftpm/
Package.resolved
*.swiftdoc
*.swiftmodule
*.swiftsourceinfo

# Xcode
DerivedData/
build/
*.xcuserstate
*.xcuserdatad/
xcuserdata/
*.xcscmblueprint
*.xccheckout
*.moved-aside

# Generated by xcodegen from App/project.yml — do not commit.
App/macsh.xcodeproj/
App/build/

# Release artifacts (DMGs produced by scripts/build-release.sh).
dist/

# macOS / Finder
.DS_Store
.AppleDouble
.LSOverride
Icon?
._*

# Editors
.vscode/
.idea/
*.swp
*.swo
*~

# Logs / temp
*.log
*.tmp

# Project artifacts that must be downloaded locally
# (universal rclone binary; fetch with ./scripts/fetch-rclone.sh)
App/macsh/Resources/rclone

# Local fixture from Tests/Integration/sftp_mount_test.sh
# (lives in $HOME, but if someone redirects it into the repo we ignore it)
sftp-data/

# Internal design/planning scratch — not part of the public repo.
docs/superpowers/

# Architecture diagram source (d2 + working files). The rendered
# PNG/SVG live under docs/architecture/ and are committed.
/architecture/

# Dev-only screenshot helper. Run locally to capture README images.
scripts/capture-screenshots.swift

# Captured screenshots before being curated/renamed to assets/screenshot.png.
assets/screenshot-*.png
